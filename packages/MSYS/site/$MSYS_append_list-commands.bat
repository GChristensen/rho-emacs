@rem package:MSYS

@echo   A set of UNIX commands from MSYS, including: ls, cat, cp, mv, gzip, ...
@echo.
@echo   A set of commands from MinGW (when installed), including:
@echo     gcc
@echo     mingw32-make
@echo     perl
@echo     autoconf (MSYS only)
@echo     automake (MSYS only)
@echo     etc.
@echo.
